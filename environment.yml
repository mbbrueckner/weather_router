name: weather-router
channels:
  - defaults
dependencies:
  - python>=3.10
  - numpy
  - jupyter
  - ipywidgets>=8.0
  - pip
  - pip:
    - gpxpy
    - openmeteo-requests
    - openmeteo-sdk
    - rdp
    - folium
    - voila
    - pytest
variables:
prefix:
platforms:
  - linux-64
  - osx-arm64
  - win-64
  - osx-64
