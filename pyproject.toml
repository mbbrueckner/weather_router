[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "weather-router"
version = "1.0.0"

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]
